
# Web scraping (optional — pipeline works with .txt files)
requests>=2.31.0
beautifulsoup4>=4.12.0
praw>=7.7.0
# Chunking
langchain>=0.1.0
langchain-text-splitters>=0.0.1
# Embedding — runs locally, no API key needed
sentence-transformers==3.4.1
torch>=2.0.0
# Vector store — runs locally, no account needed
chromadb>=0.6.0
# LLM — Groq free tier
groq==0.15.0
# Web UI
gradio>=4.0.0
# Utilities
numpy>=1.24.0
tqdm>=4.65.0
python-dotenv==1.0.1



# Query interface — add whichever you use for Milestone 5:
# gradio>=6.9.0
# streamlit>=1.40.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
